service vendor.counter-hal-default /vendor/bin/hw/android.hardware.counter-service
    class hal
    user system
    group system
    oneshot
    socket counter_hal_stream seqpacket 0666 system system
    seclabel u:object_r:hal_counter_default_exec:s0
